export const environment = {
  clientCode: '{{CLIENT_CODE}}',
  okta: {
    clientId: '{{OKTA_CLIENT_ID}}',
    issuer: '{{OKTA_ISSUER}}',
    samlIdentityProviderId: '{{OKTA_SAML_IDENTITY_PROVIDER_ID}}'
  }
};
